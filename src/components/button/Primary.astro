---
const { href, name, target, radius,} = Astro.props;
---

<a
  href={href}
  class=`${radius} py-2 px-6 bg-dark-800 dark:bg-white hover:bg-dark-500 dark:hover:bg-dark-300 text-base text-white dark:text-black duration-1000`
  target={target}>{name}</a
>
